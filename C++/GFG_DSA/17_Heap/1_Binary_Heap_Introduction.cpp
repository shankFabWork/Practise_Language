#include <iostream>

using namespace std;

// Binary Heap
// use in heap sort
// used to implement priority queue
// it is of two types:
// min heap -> 
// the key at root must be minimum among all keys 
// max heap -> 
// the key at root must be maximum among all keys 


int main()
{
    int arr[]={};
    int n=sizeof(arr)/sizeof(int);

    //Time Complexity -> 
    //Space Complexity -> 

    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<'-';
    }
    
    return 0;
}


