// An array is a collection of items of same data type
// stored at contiguous memory locations. This makes it easier to
// calculate the position of each element by simply adding an offset to
// a base value, i.e., the memory location of the first element of the
// array (generally denoted by the name of the array).

#include <iostream>

using namespace std;

// Array
// 1) Random Access
// 2) Code friendliness
// (for small array , it may be stored 
// in cache according to the cache size)

int main()
{


    return 0;
}
